form.authform(action="/", method="POST")
    input(type="hidden", name="route" value="signin")
    label(for="login") Логин:
    input.authform__login(type="text", name="login" id="login" required)
    label(for="password") Пароль:
    input.authform__password(type="password", name="password" id="password" required)
    button(type="submit") Войти
    